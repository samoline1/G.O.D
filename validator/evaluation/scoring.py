from validator.evaluation.docker_evaluation import run_evaluation_docker

    
# starting to sketch it out 
#def score_miners(Task):
    # miners = get all miners 
#    for miner in miners: 
        #last commit = get_last_commit(miner.id)
        #loss, is_finetune = run_evaluation_docker(last_commit, task)
    
